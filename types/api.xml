<types ns="_artdeco">
  <method name="compileStylesheetsSync" return="_artdeco.ClosureReturn">
    <arg type="(string|!Array<string>)" name="css">
      The resolved path to the CSS file to compile.
    </arg>
    <arg type="!_artdeco.ClosureStylesheetsConfig" name="config">
      Additional configuration to transform into arguments to Java.
      Requires at list path to the JAR file.
    </arg>
    <arg opt type="!Function" name="log">
      The logging function.
    </arg>
    Compiles stylesheets in a sync manner.
  </method>

  <method async name="compileStylesheets" return="_artdeco.ClosureReturn">
    <arg type="(string|!Array<string>)" name="css">
      The resolved path to the CSS file to compile.
    </arg>
    <arg type="!_artdeco.ClosureStylesheetsConfig" name="config">
      Additional configuration to transform into arguments to Java.
      Requires at list path to the JAR file.
    </arg>
    <arg opt type="!Function" name="log">
      The logging function.
    </arg>
    Compiles stylesheets asynchronously.
  </method>
</types>